# 198 — GitHub publication handoff

Classification: `RECONSTRUCTED_REPOSITORY_HANDOFF`

A fresh Git lineage was created under GitHub user `shim80` because original workspace Git state was repeatedly lost during infrastructure remounts.

The publication preserves the recovered PSP source, historical causal identifiers, agent/contributor directives, resume/status documents and the P1 GETAWAIT checkpoint while excluding proprietary game data.

Historical SHAs remain provenance records rather than fabricated parents of the reconstructed Git lineage.
