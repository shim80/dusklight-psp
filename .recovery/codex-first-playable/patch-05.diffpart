 prepare_tree() {
   local destination="$1"
   safe_mkdir "${destination#"$PROJECT_ROOT"/}/data/common"
@@ -68,11 +85,15 @@ prepare_tree() {
   done
   for package in \
     startup.dpst startup_logos.dpsu title_ui.dpsu \
-    file_select.dpsu \
+    file_select.dpsu title_camera.dpcm \
     title_room.dprm title_room.dptx \
     title_logo.dprm title_logo.dptx title_logo.dpan; do
     cp -- "$STARTUP/$package" "$destination/data/startup/$package"
   done
+  cp -- "$FSP102/fsp102_environment.dprm" \
+    "$destination/data/startup/fsp102_environment.dprm"
+  cp -- "$FSP102/fsp102_environment.dptx" \
+    "$destination/data/startup/fsp102_environment.dptx"
   printf '%s\n' \
     "owner=global" \
     "source=data/common/hud.dpui" \
@@ -242,6 +263,7 @@ manifest_entry() {
 }
 
 ensure_startup_assets
+ensure_fsp102_environment_assets
 "$SCRIPT_DIR/build-first-room-transition-assets.sh"
 rm -rf -- "$ACTOR"
 safe_mkdir build/assets/original-rendered-actor
diff --git a/scripts/build-dusklight-startup-assets.sh b/scripts/build-dusklight-startup-assets.sh
index 355318c..244f1e0 100755
--- a/scripts/build-dusklight-startup-assets.sh
+++ b/scripts/build-dusklight-startup-assets.sh
@@ -24,6 +24,39 @@ cmake --build "$BUILDER_BUILD"
 
 PROBE="$PROJECT_ROOT/build/host/link-loader/probe/dusk_link_loader_probe"
 BUILDER="$BUILDER_BUILD/dusk_startup_builder"
+"$SCRIPT_DIR/bootstrap-fsp102-exporter.sh" >/dev/null
+GCLIB_ROOT="$(assert_project_path .tools/sources/gclib/64127742467acb633d51685b9b1798ab45bb4034)"
+[ -d "$GCLIB_ROOT/gclib" ] ||
+  die "source gclib épinglée absente sous .tools"
+DEMO38_SOURCE_DIR="$(assert_project_path .tmp/demo38-camera-source)"
+DEMO38_SOURCE="$DEMO38_SOURCE_DIR/demo38_01.stb"
+safe_mkdir "$DEMO38_SOURCE_DIR"
+env \
+  DUSKLIGHT_GAME_IMAGE="$DUSKLIGHT_GAME_IMAGE" \
+  DUSKLIGHT_LIST_RARC_PATH=/res/Object/Demo38_01.arc \
+  DUSKLIGHT_RARC_RESOURCE_NAME=demo38_01.stb \
+  DUSKLIGHT_RARC_RESOURCE_OUTPUT="$DEMO38_SOURCE" \
+  http_proxy=http://127.0.0.1:9 \
+  https_proxy=http://127.0.0.1:9 \
+  ALL_PROXY=http://127.0.0.1:9 \
+  NO_PROXY= \
+  "$PROBE" >"$DEMO38_SOURCE_DIR/extract.log"
+"$SCRIPT_DIR/build-demo38-camera-assets.sh" "$DEMO38_SOURCE"
+CAMERA="$(assert_project_path build/assets/demo38-camera)"
+TITLE_SOURCE_DIR="$(assert_project_path .tmp/title-logo-source)"
+safe_mkdir .tmp/title-logo-source
+for title_resource in titlelogo_tm.bmd titlelogo.bck; do
+  env \
+    DUSKLIGHT_GAME_IMAGE="$DUSKLIGHT_GAME_IMAGE" \
+    DUSKLIGHT_LIST_RARC_PATH=/res/Object/TitlePal.arc \
+    DUSKLIGHT_RARC_RESOURCE_NAME="$title_resource" \
+    DUSKLIGHT_RARC_RESOURCE_OUTPUT="$TITLE_SOURCE_DIR/$title_resource" \
+    http_proxy=http://127.0.0.1:9 \
+    https_proxy=http://127.0.0.1:9 \
+    ALL_PROXY=http://127.0.0.1:9 \
+    NO_PROXY= \
+    "$PROBE" >"$TITLE_SOURCE_DIR/extract-$title_resource.log"
+done
 OUTPUT="$(assert_project_path "build/assets/dusklight-startup")"
 PASS1="$(assert_project_path "build/assets/dusklight-startup-pass1")"
 PASS2="$(assert_project_path "build/assets/dusklight-startup-pass2")"
@@ -33,7 +66,6 @@ done
 
 BASE_FILES=(
   title_room.dprm title_room.dptx
-  title_logo.dprm title_logo.dptx title_logo.dpan
   startup_logos.dpsu title_ui.dpsu
 )
 REUSE_BASE=true
@@ -53,6 +85,7 @@ done
 
 convert_pass() {
   local destination="$1"
+  cp -- "$CAMERA/title_camera.dpcm" "$destination/title_camera.dpcm"
   if [ "$REUSE_BASE" = true ]; then
     for file in "${BASE_FILES[@]}"; do
       cp -- "$OUTPUT/$file" "$destination/$file"
@@ -72,19 +105,6 @@ convert_pass() {
     "$PROBE" >"$destination/title_room.export.log"
     env \
     DUSKLIGHT_GAME_IMAGE="$DUSKLIGHT_GAME_IMAGE" \
-    DUSKLIGHT_STARTUP_TITLE_EXPORT=1 \
-    DUSKLIGHT_ROOM_TEXTURE_MAX=128 \
-    DUSKLIGHT_ROOM_SOURCE_TEXTURE_MAX=2048 \
-    DUSKLIGHT_TITLE_DPRM_OUTPUT="$destination/title_logo.dprm" \
-    DUSKLIGHT_TITLE_DPTX_OUTPUT="$destination/title_logo.dptx" \
-    DUSKLIGHT_TITLE_DPAN_OUTPUT="$destination/title_logo.dpan" \
-    http_proxy=http://127.0.0.1:9 \
-    https_proxy=http://127.0.0.1:9 \
-    ALL_PROXY=http://127.0.0.1:9 \
-    NO_PROXY= \
-    "$PROBE" >"$destination/title_logo.export.log"
-    env \
-    DUSKLIGHT_GAME_IMAGE="$DUSKLIGHT_GAME_IMAGE" \
     DUSKLIGHT_STARTUP_UI_EXPORT=1 \
     DUSKLIGHT_STARTUP_LOGOS_DPSU_OUTPUT="$destination/startup_logos.dpsu" \
     DUSKLIGHT_STARTUP_TITLE_UI_DPSU_OUTPUT="$destination/title_ui.dpsu" \
@@ -94,6 +114,11 @@ convert_pass() {
     NO_PROXY= \
     "$PROBE" >"$destination/startup_ui.export.log"
   fi
+  GCLIB_ROOT="$GCLIB_ROOT" python3 \
+    "$PROJECT_ROOT/tools/title_logo_export.py" \
+    "$TITLE_SOURCE_DIR/titlelogo_tm.bmd" \
+    "$TITLE_SOURCE_DIR/titlelogo.bck" \
+    "$destination" >"$destination/title_logo.export.log"
   env \
     DUSKLIGHT_GAME_IMAGE="$DUSKLIGHT_GAME_IMAGE" \
     DUSKLIGHT_FILE_SELECT_UI_EXPORT=1 \
@@ -129,6 +154,7 @@ convert_pass "$PASS1"
 convert_pass "$PASS2"
 
 FILES=(
+  title_camera.dpcm
   title_room.dprm title_room.dptx
   title_logo.dprm title_logo.dptx title_logo.dpan
   startup_logos.dpsu title_ui.dpsu file_select.dpsu
@@ -153,6 +179,11 @@ done
     "source_title_archive=/res/Object/TitlePal.arc" \
     "source_title_model_id=10" \
     "source_title_bck_id=7" \
+    "source_camera_archive=/res/Object/Demo38_01.arc" \
+    "source_camera_resource=evt/demo38_01.stb" \
+    "source_camera_fps=30" \
+    "source_camera_frames=2400" \
+    "source_camera_samples=2401" \
     "texture_max_dimension=128" \
     "deterministic=true" \
     "title_material_animation_status=unsupported_ids_13_16_19" \
diff --git a/scripts/build-fsp102-environment-assets.sh b/scripts/build-fsp102-environment-assets.sh
new file mode 100755
index 0000000..dab8cee
--- /dev/null
+++ b/scripts/build-fsp102-environment-assets.sh
@@ -0,0 +1,30 @@
+#!/usr/bin/env bash
+set -euo pipefail
+SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+. "$SCRIPT_DIR/lib/common.sh"
+require_project_root
+SOURCE_ROOT="${DUSKLIGHT_FSP102_SOURCE_ROOT:-}"
+[ -n "$SOURCE_ROOT" ] || die "DUSKLIGHT_FSP102_SOURCE_ROOT is required"
+[ -d "$SOURCE_ROOT/R00/bmdr" ] || die "missing F_SP102 R00 source models"
+[ -d "$SOURCE_ROOT/STG/bmdp" ] || die "missing F_SP102 stage source models"
+GCLIB_COMMIT="64127742467acb633d51685b9b1798ab45bb4034"
+GCLIB_ROOT="$(assert_project_path ".tools/sources/gclib/$GCLIB_COMMIT")"
+if [ ! -d "$GCLIB_ROOT/.git" ]; then
+  "$SCRIPT_DIR/bootstrap-fsp102-exporter.sh" >/dev/null
+fi
+OUT="$(assert_project_path "build/assets/fsp102-environment")"
+PASS1="$(assert_project_path ".tmp/fsp102-environment-pass1")"
+PASS2="$(assert_project_path ".tmp/fsp102-environment-pass2")"
+rm -rf -- "$PASS1" "$PASS2" "$OUT"
+mkdir -p -- "$PASS1" "$PASS2" "$OUT"
+run_pass() {
+  GCLIB_ROOT="$GCLIB_ROOT" python3 "$PROJECT_ROOT/tools/fsp102_environment_export.py" "$SOURCE_ROOT" "$1"
+}
+run_pass "$PASS1"
+run_pass "$PASS2"
+for file in fsp102_environment.dprm fsp102_environment.dptx FSP102_ENVIRONMENT.MANIFEST; do
+  cmp "$PASS1/$file" "$PASS2/$file"
+  cp -- "$PASS1/$file" "$OUT/$file"
+done
+sha256sum "$OUT/fsp102_environment.dprm" "$OUT/fsp102_environment.dptx" > "$OUT/FSP102_ENVIRONMENT.SHA256"
+printf 'FSP102_ENVIRONMENT_ASSETS_OK deterministic=true models=9\n'
diff --git a/test/canonical-assets-host/main.cpp b/test/canonical-assets-host/main.cpp
index 298df07..f0200ec 100644
--- a/test/canonical-assets-host/main.cpp
+++ b/test/canonical-assets-host/main.cpp
@@ -52,9 +52,9 @@ int main() {
                 "title_ui_path") ||
         !expect(std::strcmp(startup.file_select_ui, "data/startup/file_select.dpsu") == 0,
                 "file_select_ui_path") ||
-        !expect(std::strcmp(startup.title_room_model, "data/startup/title_room.dprm") == 0,
+        !expect(std::strcmp(startup.title_room_model, "data/startup/fsp102_environment.dprm") == 0,
                 "title_room_model_path") ||
-        !expect(std::strcmp(startup.title_room_textures, "data/startup/title_room.dptx") == 0,
+        !expect(std::strcmp(startup.title_room_textures, "data/startup/fsp102_environment.dptx") == 0,
                 "title_room_texture_path") ||
         !expect(std::strcmp(startup.title_logo_model, "data/startup/title_logo.dprm") == 0,
                 "title_logo_model_path") ||
diff --git a/test/canonical-runtime/CMakeLists.txt b/test/canonical-runtime/CMakeLists.txt
index ad4d176..262526b 100644
--- a/test/canonical-runtime/CMakeLists.txt
+++ b/test/canonical-runtime/CMakeLists.txt
@@ -699,3 +699,15 @@ target_compile_features(alpha_material_state_host_test PRIVATE cxx_std_20)
 target_compile_options(alpha_material_state_host_test PRIVATE
     -Wall -Wextra -Werror -fsigned-char -fno-exceptions -fno-rtti
 )
+
+add_executable(material_pass_plan_host_test
+    material_pass_plan_host_test.cpp
+    "${DUSK_PSP_ROOT}/src/room_package.cpp"
+)
+target_include_directories(material_pass_plan_host_test PRIVATE
+    "${DUSK_PSP_ROOT}/include"
+)
+target_compile_features(material_pass_plan_host_test PRIVATE cxx_std_20)
+target_compile_options(material_pass_plan_host_test PRIVATE
+    -Wall -Wextra -Werror -fsigned-char -fno-exceptions -fno-rtti
+)
diff --git a/test/canonical-runtime/startup_title_asset_host_test.cpp b/test/canonical-runtime/startup_title_asset_host_test.cpp
index 6474a16..84cf899 100644
--- a/test/canonical-runtime/startup_title_asset_host_test.cpp
+++ b/test/canonical-runtime/startup_title_asset_host_test.cpp
@@ -1,5 +1,6 @@
 #include "dusk/psp/room_package.hpp"
 
+#include <cmath>
